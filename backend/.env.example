# Supabase project settings (Project Settings -> API)
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=<service-role-key>   # server-side only, bypasses RLS - never expose to the frontend
SUPABASE_JWT_SECRET=<jwt-secret>               # Project Settings -> API -> JWT Settings, used to verify user access tokens

# Supabase Storage
SUPABASE_STORAGE_BUCKET=images

# CORS
FRONTEND_ORIGIN=https://localhost

# 로깅/환경 (선택, 기본값 있음)
ENVIRONMENT=development
LOG_LEVEL=INFO

# 인증 (MVP: 로그인 화면 미구현). true면 JWT 검증을 건너뛴다 — 로컬/시연 전용
AUTH_DISABLED=true

# 시연용 결제 실패(402) 경로. true면 x-mock-payment-failure 헤더로 카드 승인 거절을
# 재현할 수 있다. AUTH_DISABLED와 분리돼 있으니 시연 때만 켤 것.
MOCK_PAYMENT_FAILURE=false

# AI 추론 서버. 모델은 외부에서 API로 못 닿는 망에 있어 팀원 Mac에서 띄우고
# ngrok 고정 도메인으로 노출한다. 비우면 recognize는 501(미연결)을 유지한다.
MODEL_API_URL=https://<your-domain>.ngrok-free.dev
MODEL_API_TIMEOUT=30
# 이 값(0~100) 미만이면 is_below_threshold / needs_review = true
AI_CONFIDENCE_THRESHOLD=70

# 시연용: 카메라 없이 촬영 버튼만 눌러 image_path 없이 세션을 만들면, 이 버킷
# **루트**에 있는 이미지를 대신 추론한다. 파일 목록은 촬영할 때마다 조회하므로
# Storage에 사진을 올리거나 지우면 그대로 반영된다 - 여기를 고칠 필요가 없다.
# 하위 폴더는 조회 대상이 아니라, 원본 보관용 폴더를 같은 버킷에 둬도 안전하다.
# 비워두면 이미지 없는 세션은 failure_reason="NO_IMAGE"로 실패한다.
#
# 루트에는 원본(수 MB) 대신 긴 변 1920px 리사이즈본을 올릴 것. 모델이 signed URL로
# 매번 내려받으므로, 탐지 결과는 사실상 같으면서 다운로드가 4~9배 줄어든다.
DEMO_SCAN_IMAGE_BUCKET=test
